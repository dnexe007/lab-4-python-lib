Тип:
* неверное логическое условие

Место:
* `game_collection.py`, строка 91

Симптом:
* при покупке игры в ходе симуляции вылетает исключение `ValueError("Game is not in collection")`. Ошибка вылетает при попытке удалить игру из списковой коллекции внутри словаря по id

Как воспроизвести:
* Симуляция `sm 0 100 0`

Отладка:
* Breakpoint: `game_dict.py`, строка 94 `self._dct[key].remove_game(game)`
* Информация в отладчике: удаляемая игра - `The Last of Us`. Показано, что игра есть внутри `_dct`, но все равно вызывается ошибка

Причина:
* `game_collection.py`, строка 91: `if game in self._games:`. неверная проверка условия для вызова исключения - отутствует `not`

Исправление:
* заменить на `game not in self._games:`

Проверка:
* программа больше не выдаёт исключений

Доказательства:
* `debugger.png`
* `breakpoint.png`
* `exception.png`
